# 安装目录与文件架构（as-built）

> **日期：** 2026-09-10  
> **契约：** [identity.md](identity.md) · [ADR-v6-031](adr/ADR-v6-031.md)

安装根与产品家园分离。Windows per-user 载荷在 `%LOCALAPPDATA%\Programs\YohuAdbTools`，可变文件在 `%LOCALAPPDATA%\YohuAdbTools`（`config` / `data` / `cache` / `logs`）。改 `data_root` 不搬家。无旧布局迁移。

详见 identity 路径规划树。
