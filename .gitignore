# ============================================================================
# Yohu ADB Tools — Git 忽略
# ============================================================================

# —— 操作系统 ——
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/
.DS_Store

# —— 编辑器 / IDE ——
.vs/
.idea/
*.swp
*.swo
*~
*.bak
*.orig
*.user
*.suo

# —— 环境与密钥（示例文件可入库） ——
.env
.env.*
!.env.example

# —— 本地临时目录（截图、联调、一次性产物、外部研究缓存） ——
.tmp*/
.tmp-*/
.ui-review/
/temp/
/tmp/

# —— 日志（仅仓库根 Logs/；勿写成 logs/ 以免误伤 ui/packages/modules/logs/） ——
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*
/Logs/

# —— .NET 构建产物 ——
bin/
obj/

# —— 运行时/用户配置（不提交，避免覆盖他人环境） ——
Config/

# —— 发布输出 ——
publish/
publish_*/
publish-*/

# —— Rust ——
/target/
*.rs.bk

# —— Node / pnpm / Vite / Turbo ——
node_modules/
*.tsbuildinfo
dist/
.turbo/
.vite/
coverage/

# —— Sidecar 工具二进制（不入库；scripts/setup-adb.ps1 从 Google platform-tools 下载） ——
# tools/fake-adb/ 源码正常入库；E2E 走 UIA，不再用 msedgedriver
/tools/adb.exe
/tools/adb
/tools/AdbWinApi.dll
/tools/AdbWinUsbApi.dll
/tools/scrcpy-server
/tools/ffmpeg/
