name: CI

# Yohu ADB Tools v6 — 持续集成与发布流水线（架构文档 §11.3）
# 触发：push / PR 到 main；手动 workflow_dispatch。
# 验收门槛与 CLAUDE.md「发布检查清单」一致（1/2/3/7 项自动化）。

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always

jobs:
  verify:
    name: 验证（Rust + 前端）
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Rust 工具链
        uses: dtolnay/rust-toolchain@stable

      - name: Rust 缓存
        uses: swatinem/rust-cache@v2

      - name: sidecar adb（下载官方 platform-tools）
        shell: powershell
        run: powershell -ExecutionPolicy Bypass -File scripts/setup-adb.ps1

      - name: sidecar scrcpy-server（投屏资源）
        shell: powershell
        run: powershell -ExecutionPolicy Bypass -File scripts/setup-scrcpy-server.ps1

      - name: pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Node
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm
          cache-dependency-path: ui/pnpm-lock.yaml

      - name: 前端依赖
        working-directory: ui
        run: pnpm install --frozen-lockfile

      - name: 前端类型检查
        working-directory: ui
        run: pnpm exec tsc -b

      - name: 前端测试（Vitest）
        working-directory: ui
        run: pnpm test

      - name: token 纪律（ADR-v6-011：禁硬编码色值/字号）
        working-directory: ui
        run: pnpm lint

      - name: 前端生产构建（PR 也校验可构建；体积断言在 package）
        working-directory: ui
        run: pnpm build

      - name: Rust 构建（含 fake-adb 明文 bin）
        run: cargo build --workspace

      - name: Rust 测试（含 fake-adb 集成）
        run: cargo test --workspace

      - name: Clippy（-D warnings）
        run: cargo clippy --workspace --all-targets -- -D warnings

  verify-macos:
    name: 验证 macOS（Rust + 前端）
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: Rust 工具链
        uses: dtolnay/rust-toolchain@stable

      - name: Rust 缓存
        uses: swatinem/rust-cache@v2

      - name: sidecar adb
        run: bash scripts/setup-adb.sh

      - name: sidecar scrcpy-server
        run: bash scripts/setup-scrcpy-server.sh

      - name: pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Node
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm
          cache-dependency-path: ui/pnpm-lock.yaml

      - name: 前端依赖
        working-directory: ui
        run: pnpm install --frozen-lockfile

      - name: 前端类型检查
        working-directory: ui
        run: pnpm exec tsc -b

      - name: 前端测试（Vitest）
        working-directory: ui
        run: pnpm test

      - name: token 纪律
        working-directory: ui
        run: pnpm lint

      - name: Rust 构建（含 fake-adb 明文 bin）
        run: cargo build --workspace

      - name: Rust 测试（含 fake-adb 集成）
        run: cargo test --workspace

      - name: Clippy（-D warnings）
        run: cargo clippy --workspace --all-targets -- -D warnings

  package:
    name: 打包（NSIS）
    runs-on: windows-latest
    needs: verify
    if: github.event_name == 'push' || github.event_name == 'workflow_dispatch'
    steps:
      - uses: actions/checkout@v4

      - name: Rust 工具链
        uses: dtolnay/rust-toolchain@stable

      - name: Rust 缓存
        uses: swatinem/rust-cache@v2

      - name: sidecar adb
        shell: powershell
        run: powershell -ExecutionPolicy Bypass -File scripts/setup-adb.ps1

      - name: sidecar scrcpy-server（投屏资源）
        shell: powershell
        run: powershell -ExecutionPolicy Bypass -File scripts/setup-scrcpy-server.ps1

      - name: pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Node
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm
          cache-dependency-path: ui/pnpm-lock.yaml

      - name: 前端依赖 + 构建
        working-directory: ui
        run: pnpm install --frozen-lockfile && pnpm build

      - name: tauri-cli
        run: cargo install tauri-cli --locked

      - name: 打包 NSIS
        working-directory: app/yohu-adbtools
        run: cargo tauri build

      - name: 记录安装包体积
        shell: powershell
        run: |
          $setup = Get-ChildItem -Path target, app -Recurse -Filter "*setup.exe" -ErrorAction SilentlyContinue |
            Where-Object { $_.FullName -match '[\\/]bundle[\\/]nsis[\\/]' } |
            Sort-Object LastWriteTime -Descending |
            Select-Object -First 1
          if (-not $setup) { throw "未找到安装包产物" }
          $mb = [math]::Round($setup.Length / 1MB, 2)
          Write-Host "安装包：$($setup.FullName) = $mb MB"

      - name: 上传安装包产物
        uses: actions/upload-artifact@v4
        with:
          name: YohuAdbTools-installer
          path: |
            target/**/nsis/*setup.exe
            app/**/nsis/*setup.exe
          if-no-files-found: error
